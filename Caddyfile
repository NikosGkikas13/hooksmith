# Production: HTTPS via Let's Encrypt, auto-renewed by Caddy.
# Requires DNS for odyhook.dev (and www) to point at this server's IP.

odyhook.dev {
    reverse_proxy web:3000
}

# Redirect www → apex (canonical URL).
www.odyhook.dev {
    redir https://odyhook.dev{uri} permanent
}
